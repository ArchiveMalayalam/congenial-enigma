version: '3.8'

services:
  jekyll:
    build:
      context: ./jekyll
      dockerfile: ../.devcontainer/Dockerfile
    ports:
      - "4000:4000"
    restart: unless-stopped
    environment:
      - JEKYLL_ENV=production
    volumes:
      - .:/workspace
    command: sleep infinity
